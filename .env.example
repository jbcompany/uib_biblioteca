# Application environment
APP_ENV=dev

# Development database
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=biblioteca

# Test database (used when APP_ENV=test)
TEST_DB_HOST=localhost
TEST_DB_USER=root
TEST_DB_PASS=
TEST_DB_NAME=biblioteca_test
